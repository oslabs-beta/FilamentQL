type Todo {
  id: ID!
  text: String!
  isCompleted: Boolean!
  number: Int
}
type Query {
  todos: [Todo!]!
}
input addTodoInput {
  id: ID
  text: String!
  isCompleted: Boolean
}
type Mutation {
  addTodo(input: String!): Todo
}
